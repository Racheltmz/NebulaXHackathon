# Submission

`predictions.zip` — `door_predictions.csv`, `acv_predictions.csv`, `rail_predictions.csv`, `shm_predictions.csv` at the top level, in the organiser format.

The Door, Rail and SHM files are the models' own predictions (public-test scores 0.9474, 0.8080, 0.9322). The ACV ranking in this zip was set
manually (car 01 first); the model's own ACV output scored 0.375.
